---
- hosts: all
  gather_facts: false
  tasks:
    - name: Deploy Kubernetes Deployment
      kubernetes.core.k8s:
        state: present
        manifest: "deployment.yml"
        kubeconfig: "/root/.kube/config"
